CREATE DATABASE mysql_benchmark;


 CREATE TABLE benchmark_table ( 
  id INT AUTO_INCREMENT PRIMARY KEY,
  nome VARCHAR(50), 
  indirizzo VARCHAR(100), 
  numero_telefono VARCHAR(20),
  email VARCHAR(100)
 );
